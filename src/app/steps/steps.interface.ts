export interface StepPoint {
  index: string;
  title: string;
  extraTitle?: string;
  description?: string;
  color?: string;
  background?: string;
  contentColor?: string;
  contentBackground?: string;
  hiddenContent?: boolean;
}
